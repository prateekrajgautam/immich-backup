# Copy to .env and adjust to match your setup.

# Path on the host to Immich's library/upload folder (read-only mount).
IMMICH_LIBRARY_PATH=./library

# Path on the host where you want local incremental backups written
# (e.g. your mounted external drive).
LOCAL_BACKUP_PATH=./backup-destination
